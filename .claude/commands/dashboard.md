Call the `get_due_tasks` tool and display a refreshed task dashboard showing:
- Overdue tasks (highlighted)
- Tasks due today
- Tasks due this week

Then ask if the user wants to take any action on a task.
